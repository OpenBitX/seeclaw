# 技能：SoM Grid 网格回退算法与 Prompt (方案C)

如果本地视觉模型未检测到目标，系统退化为网格方案：
1. 使用 Rust 的 `imageproc` 或基础像素操作，在原始截图上绘制半透明的 N x N 网格（例如 10x10）。
2. 在每个网格中心打上坐标标签（如 A1, B5）。
3. 发送给 GLM-4.6V 的 Prompt 必须包含：“当前截图已覆盖坐标网格，请告诉我目标元素所在的网格编号(例如 C4)”。
4. 收到 VLM 回复后，Rust 取该网格模块的中心点物理坐标执行点击。

