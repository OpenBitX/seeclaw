# Git 提交流程规范

遵循 Conventional Commits 规范，所有 Commit Message 必须采用以下格式：
`<type>(<scope>): <subject>`

- `feat`: 新功能 (如 `feat(vision): add ONNX runtime for YOLO`)
- `fix`: 修复 bug
- `refactor`: 重构代码 (不增加新功能，也不修复bug)
- `chore`: 构建过程或辅助工具的变动
- `perf`: 性能优化

