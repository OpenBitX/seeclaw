[project]
name = "seeclaw-scripts"
version = "0.1.0"
description = "Python scripts for SeeClaw (YOLO export, etc.)"
requires-python = ">=3.10"
dependencies = [
    "ultralytics>=8.0",
    "onnx>=1.14",
    "onnxsim>=0.4",
    "huggingface-hub>=1.5.0",
]
